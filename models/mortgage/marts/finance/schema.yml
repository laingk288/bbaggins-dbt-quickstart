models:
  - name: fct_boc_rates_latest
    columns:
      - name: series
        tests: [not_null, unique]
      - name: rate_date
        tests: [not_null]
      - name: value
        tests: [not_null]

